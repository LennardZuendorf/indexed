"""Integration tests for full pipelines.

This package contains end-to-end integration tests for:
- Full indexing pipeline (Connector → Reader → Converter → Embedding → Storage)
- Search pipeline (Query → Embedding → FAISS → Results)
"""
