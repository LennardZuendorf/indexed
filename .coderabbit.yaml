# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
# CodeRabbit configuration — https://docs.coderabbit.ai/reference/configuration
reviews:
  path_filters:
    # Agent tooling — unvendored (gitignored); installed from upstream via
    # `npx skills`, pinned in skills-lock.json. Not authored here.
    - "!.agents/**"
    - "!.claude/**"
