# indexed-connectors

Document connectors for reading data from various sources:

- **Jira** (Cloud and Server/DC)
- **Confluence** (Cloud and Server/DC)  
- **Local Files** (with regex pattern matching)

## Features

- Automatic pagination and batching
- Retry logic with exponential backoff
- Document caching support
- Flexible authentication (tokens, basic auth)
