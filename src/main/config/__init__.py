"""Main config package for settings and storage utilities."""

# Intentionally do not import submodules here to avoid import-time issues.
# Import from submodules directly, e.g.:
# from main.config.settings import IndexedSettings
# from main.config.store import read_toml, atomic_write_toml, validate_no_secrets, get_config_path
